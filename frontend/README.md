# Frontend

La interfaz del PMV se sirve desde `backend/src/main/resources/templates` y `static`, usando Thymeleaf para el renderizado del lado servidor. Esta carpeta queda reservada para una futura separación del cliente si el proyecto lo requiere.
